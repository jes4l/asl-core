if (timeRemainingText != "") {
    scrDrawText(textX, textY, timeRemainingText, textColor, textAlpha, fontToUse);
}
if (message2Text != "") {
    scrDrawText(textX, message2y, message2Text, textColor, textAlpha, fontToUse);
}
