if (timeRemainingText != "") {
    scrDrawText(textX, textY, timeRemainingText, textColor, textAlpha, fntSevenSegment65);
}
if (message2Text != "") {
    scrDrawText(textX, message2y, message2Text, textColor, textAlpha, fntSevenSegment60);
}
