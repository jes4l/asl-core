if (timeRemainingText != "") {
    scrDrawText(textX, textY, timeRemainingText, textColor, textAlpha, fntSevenSegmentPlaces);
}
if (message2Text != "") {
    scrDrawText(textX, message2y, message2Text, textColor, textAlpha, fntSevenSegmentLetterPlaces);
}
